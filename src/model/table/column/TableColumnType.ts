/**
 * @description Table Column Type ID
 * recommended to keep it alphabetized to avoid duplication if you've got a lot of table columns
 */

export enum TableColumnType {
    EXAMPLEONLY,
    NAME,
    SPECIALTIES,
    UNKNOWN
}
